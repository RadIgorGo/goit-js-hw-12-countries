<div>
    <h3>{{name}}</h3>
    <p>Capital:{{capital}}</p>
    <p>Population:{{population}}</p>
    <p>languages:</p>
    <ul>
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
    <img src="{{flag}}" alt="{{name}}" width="350">
</div>